int echoInteger(int value);

/**
 * Basic example which only serves to clarify the steps necessary to call functions written in C programming language.
 * Such as, for example, the casting of data types from PHP to C and vice versa,
 * as well as the association between the underlying C function and the overlying PHP method.
 */

/**
 * This function simply echoes the parameter passed to it.
 */
inline int echoInteger(int value) {
    // In principle, from here on, any type of processing can follow.
    // For example: the use of system or numerical analysis libraries, or the activation of some actuator connected to the LAMP server.
    return value;
}
